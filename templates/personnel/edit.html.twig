{% extends 'base.html.twig' %}

{% block title %}Edit Personnel{% endblock %}
{% block styles %}
{{ parent() }}
    <style>
        .container{
            width: 400px;
            margin-top:100px;
        }
    </style>
{% endblock %}
{% block body %}
<div class="container">
    <div class="card text-primary">
      <div class="card-body">
        <div>
            <h4 class="card-title text-center">Modification du personnel</h4>
            {{ include('personnel/_form.html.twig', {'btn_label': 'Mettre à jour'}) }}    
        </div>
      </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}  
    <script>
        // active current page 
        $(document).ready(()=>{
            activeCurrentPage('personnel');
        });
        //end
    </script>
{% endblock %}